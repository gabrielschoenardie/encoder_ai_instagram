# ============================================================================
# Instagram Reels Encoder - CINEON FILM EMULATION EDITION v2.1.0
# ============================================================================
# A lista de dependencias vive em `pyproject.toml` ([project] dependencies +
# [project.optional-dependencies]). Este arquivo existe apenas porque
# MANUAL_INSTALACAO.txt manda o usuario final rodar
# `pip install -r requirements.txt` — nao "conserte" reexpandindo a lista
# aqui; edite o pyproject.toml.
-e .[opencv]
